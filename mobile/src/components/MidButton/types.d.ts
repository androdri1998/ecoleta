export interface IPropsIndex {
  onClickButon: (pointerInside: boolean) => void;
  textButton: string;
  iconButton: JSX.Element;
}

export interface IPropsRender {
  onClickButon: (pointerInside: boolean) => void;
  textButton: string;
  iconButton: JSX.Element;
}
